---
description: 电动车智控系统 — 长期项目背景、架构原则与修改边界（始终应用）
alwaysApply: true
---

# 电动车智控系统（长期记忆 / 项目约定）

## 项目是什么

- **名称**：电动车智控系统（工作区：`智控系统`）。
- **目标**：接入厂家提供的 **808 协议**（JT/T 808 或厂家扩展）；系统需支持 **后续绑定其他厂家硬件**（通过适配层扩展，不绑死单一厂商）；架构上预留 **未来做成 App**（业务走统一后端 API，终端只对接 API）。
- **代码与协作**：以 **Git 提交 + 远程仓库** 为权威备份；助手不代替用户做托管，但改代码须便于用户自行提交。

## 架构原则（助手实现功能时默认遵守）

- **协议与设备**：808 解析、鉴权、厂商差异放在 **adapter / 协议层**；上层用稳定领域模型（车辆、终端、轨迹、告警、指令等）。
- **多端**：车主侧为 **uni-app（微信小程序 + 可打包 App）**；PC 管理端为 `apps/admin-web`。均只依赖 **HTTP/WebSocket 等与设备无关的 API**；设备长连接落在服务端。
- **扩展新厂家**：优先新增适配器与配置，避免复制粘贴整套业务逻辑。

## 修改边界（用户明确要求）

- **未点名改动的行为一律不动**：用户只要求某一块（如某页布局、某接口、某文件）时，不得擅自改其他功能、流程、跳转、数据结构与业务逻辑；不得以「优化」「对齐」「顺手」为由替换、合并或删减既有实现。
- **未经用户点名文件/范围，不得** 做「顺手重构」、格式化全项目、改无关依赖或删改用户未提及的逻辑。
- **若任务涉及多处文件**，先说明将动哪些路径，或仅在用户已同意的范围内修改。
- **用户补充协议文档、报文样例时**：优先落到约定目录（如 `docs/protocol/` 或用户指定路径），不擅自移动用户已有文件结构。

## 沟通语言

- 用户主要使用 **中文**；解释与注释可与代码库现有语言一致。

## 硬件与产品功能（用户已确认，须全部落地）

- **单一事实来源**：`docs/全量功能规格.md`（三张产品图 + WZ808 V2.8 合并的完整开发与验收清单）；硬件原文摘见 `docs/hardware-capabilities.md`。
- **范围**：实时/定时跟踪、轨迹回放（目标 365 天）、超速/震动/围栏/主电与拆除等告警、可配置上报、车队与多用户、未来 App 地图与通知；传输为 **TCP + 808**。
- **实现顺序**：可与用户分期，但验收清单中的条目最终均需实现；808 消息号与私有字段以 `docs/protocol/` 厂家资料为准，部标仅作对照。
- **当前厂家协议**：`docs/protocol/WZ标准版808协议V28.pdf`（WZ 通用版部标 **V2.8**）；摘要 `docs/protocol/WZ-808-V28-summary.md`。
