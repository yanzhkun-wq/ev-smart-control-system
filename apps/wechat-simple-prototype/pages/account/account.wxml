<scroll-view scroll-y class="wrap">
  <view class="card">
    <text class="t">账号设置</text>
    <text class="s">演示版：车主手机号用于授权管理与邀请链接；正式环境由服务端账号体系接管。</text>
    <text class="s s-mt">演示超管账号手机号为 19900000000，可使用「我的 → 绑定管理」修改他人邀请关系与绑定车牌。</text>
  </view>

  <view class="card form-card">
    <text class="lab">808 平台 API 根地址</text>
    <text class="s">填写 http(s)://主机:7612 或经 Nginx 的 https 域名；与微信公众平台 request 合法域名一致。留空则仅本机演示。</text>
    <input
      class="inp"
      placeholder="https://..."
      value="{{platformApiInput}}"
      bindinput="onPlatformApiInput"
    />
    <button class="btn" bindtap="onSavePlatformApi">保存平台地址</button>
  </view>

  <block wx:if="{{isMain}}">
    <view class="card form-card">
      <text class="lab">车主绑定手机号</text>
      <input
        class="inp"
        type="number"
        maxlength="11"
        placeholder="11 位手机号"
        value="{{phoneInput}}"
        bindinput="onPhoneInput"
      />
      <button class="btn" bindtap="onSavePhone">保存</button>
    </view>
  </block>

  <block wx:else>
    <view class="card">
      <text class="s">您当前为授权用车人，绑定手机号由邀请流程写入。如需变更请联系车主重新授权。</text>
    </view>
  </block>
</scroll-view>
